import 'package:flutter/material.dart';

class GameMetadataPanel extends StatelessWidget {
  const GameMetadataPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
